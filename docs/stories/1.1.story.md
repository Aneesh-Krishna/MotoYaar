# Story 1.1 — Initialize Next.js Project

**Epic:** 01 — Foundation & Infrastructure
**Story:** 1.1
**Status:** Done
**Created:** 2026-03-15

---

## Story

**As a** developer,
**I want** the Next.js (App Router) project scaffold verified, completed, and aligned with the architecture specification,
**so that** I have a consistent, typed codebase and all required infrastructure files in place from day one.

---

## Acceptance Criteria

1. - [ ] Next.js project runs with App Router, TypeScript, ESLint, and Tailwind CSS configured
2. - [ ] `.gitignore` excludes `.env*` (except `.env.example`), `node_modules/`, `.next/`
3. - [ ] Project structure follows the architecture's unified project structure — all required directories and stub files exist
4. - [ ] `pnpm dev` starts the local dev server without errors
5. - [ ] `pnpm typecheck` passes (stub files with `// TODO` comments are acceptable)
6. - [ ] `pnpm lint` passes

---

## Dev Notes

### Context: This Is a Partially-Started Project

[Source: architecture.md#starter-template--existing-project]

> ⚠️ **IMPORTANT:** MotoYaar is NOT a blank slate. Do not scaffold a new Next.js project. The following is **already in place** and must NOT be overwritten:

**Already installed and configured:**
- Next.js 14.2 (App Router) with route groups `(auth)` and `(app)`
- TypeScript 5.4
- Tailwind CSS v3.4 (`tailwind.config.ts` ← exists)
- `src/types/index.ts` ← exists (canonical types — do not modify)
- `src/components/ui/` ← exists
- `src/components/layout/` ← exists (BottomNav, SidebarNav, TopBar)
- `src/app/(app)/layout.tsx` ← exists
- `src/app/(app)/page.tsx` ← exists (Dashboard)
- `src/app/(app)/garage/page.tsx` ← exists
- `src/app/(app)/community/page.tsx` ← exists
- `src/app/(app)/trips/page.tsx` ← exists
- `src/app/(app)/profile/page.tsx` ← exists
- `src/app/(auth)/login/page.tsx` ← exists
- `src/app/layout.tsx` ← exists (root)
- `src/app/globals.css` ← exists
- `next.config.ts` ← exists
- `components.json` ← exists (shadcn/ui)
- `public/manifest.json` ← exists (PWA)
- `src/lib/utils.ts` ← exists

**Already installed packages:** shadcn/ui (via Radix UI), Tailwind CSS v3.4, Framer Motion 11.x, Recharts 2.12, React Hook Form 7.52, Zod 3.23, Sonner 1.5, NextAuth v4.24, Lucide React 0.400

**NOT yet added (handled in subsequent stories):**
- Drizzle ORM + postgres driver (Story 1.2)
- @supabase/supabase-js (Story 1.2)
- @aws-sdk/client-s3 (Story 1.3)
- resend SDK (Story 1.4)
- @anthropic-ai/sdk (Story 1.5)
- web-push (Story 1.6)
- zustand (Story 1.2+)
- Vitest, React Testing Library, Playwright (Story 1.2+)

This story's job is to **verify the existing scaffold, fill in any missing pieces, and create the core shared infrastructure files** (`errors.ts`, `api-client.ts`, `logger.ts`) so all subsequent stories have a complete, consistent base to build on.

---

### Package Manager

[Source: architecture.md#development-workflow]

- **Package manager:** `pnpm` v9+
- **Node.js runtime:** 20+
- All commands in this story use `pnpm` (not `npm` or `yarn`)

---

### Full Target Project Structure

[Source: architecture.md#12-unified-project-structure]

The following lists what should exist after this story completes. Items marked `← exists` should already be there. Items marked `← create` need to be created. Items marked `← stub` should be created as a placeholder file with a `// TODO: Implement in Story X.X` comment.

```
src/
├── app/
│   ├── (auth)/
│   │   ├── login/page.tsx                    ← exists
│   │   └── onboarding/page.tsx               ← create (stub page component)
│   ├── (app)/
│   │   ├── layout.tsx                        ← exists
│   │   ├── page.tsx                          ← exists (Dashboard)
│   │   ├── garage/
│   │   │   ├── page.tsx                      ← exists
│   │   │   ├── new/page.tsx                  ← create (stub page component)
│   │   │   └── [id]/
│   │   │       ├── page.tsx                  ← create (stub page component)
│   │   │       └── edit/page.tsx             ← create (stub page component)
│   │   ├── community/
│   │   │   ├── page.tsx                      ← exists
│   │   │   └── [id]/page.tsx                 ← create (stub page component)
│   │   ├── trips/page.tsx                    ← exists
│   │   └── profile/
│   │       ├── page.tsx                      ← exists
│   │       └── settings/page.tsx             ← create (stub page component)
│   ├── admin/
│   │   ├── layout.tsx                        ← create (stub layout)
│   │   ├── page.tsx                          ← create (stub — analytics)
│   │   ├── reported/page.tsx                 ← create (stub)
│   │   ├── users/page.tsx                    ← create (stub)
│   │   └── settings/page.tsx                 ← create (stub)
│   ├── api/
│   │   └── auth/
│   │       └── [...nextauth]/route.ts        ← create (stub, needed for NextAuth)
│   ├── layout.tsx                            ← exists (root)
│   └── globals.css                           ← exists
├── components/
│   ├── layout/                               ← exists
│   ├── ui/                                   ← exists
│   ├── vehicles/                             ← create empty dir (add .gitkeep)
│   ├── documents/                            ← create empty dir
│   ├── expenses/                             ← create empty dir
│   ├── trips/                               ← create empty dir
│   ├── community/                            ← create empty dir
│   ├── reports/                              ← create empty dir
│   ├── notifications/                        ← create empty dir
│   └── onboarding/                           ← create empty dir
├── lib/
│   ├── auth.ts                               ← stub (Epic 2)
│   ├── db/
│   │   ├── client.ts                         ← stub (Story 1.2)
│   │   └── schema.ts                         ← stub (Story 1.2)
│   ├── errors.ts                             ← CREATE NOW (fully implemented)
│   ├── api-client.ts                         ← CREATE NOW (fully implemented)
│   ├── logger.ts                             ← CREATE NOW (fully implemented)
│   ├── validations/                          ← create empty dir
│   ├── r2.ts                                 ← stub (Story 1.3)
│   ├── resend.ts                             ← stub (Story 1.4)
│   ├── anthropic.ts                          ← stub (Story 1.5)
│   ├── push.ts                               ← stub (Story 1.6)
│   └── utils.ts                              ← exists
├── services/
│   ├── userService.ts                        ← stub
│   ├── vehicleService.ts                     ← stub
│   ├── documentService.ts                    ← stub
│   ├── expenseService.ts                     ← stub
│   ├── tripService.ts                        ← stub
│   ├── reportService.ts                      ← stub
│   ├── aiReportService.ts                    ← stub
│   ├── communityService.ts                   ← stub
│   ├── notificationService.ts                ← stub
│   ├── storageService.ts                     ← stub
│   ├── emailService.ts                       ← stub
│   ├── pushService.ts                        ← stub
│   ├── adminService.ts                       ← stub
│   ├── cronService.ts                        ← stub
│   └── api/
│       ├── vehicleApi.ts                     ← stub
│       ├── documentApi.ts                    ← stub
│       ├── expenseApi.ts                     ← stub
│       ├── tripApi.ts                        ← stub
│       ├── reportApi.ts                      ← stub
│       ├── communityApi.ts                   ← stub
│       └── notificationApi.ts                ← stub
├── stores/
│   ├── notificationStore.ts                  ← stub
│   └── uiStore.ts                            ← stub
├── hooks/
│   ├── useVehicles.ts                        ← stub
│   ├── usePushSubscription.ts                ← stub
│   └── useNotifications.ts                   ← stub
├── types/
│   └── index.ts                              ← exists (do NOT modify)
└── utils/
    ├── currency.ts                           ← stub
    ├── date.ts                               ← stub
    └── hotScore.ts                           ← stub
```

**Other root files to create/verify:**
```
.github/
└── workflows/
    └── ci.yml                                ← create
vercel.json                                   ← create
.env.example                                  ← create
drizzle/
└── migrations/                               ← create empty dir
e2e/                                          ← create empty dir
```

---

### Required `package.json` Scripts

[Source: architecture.md#development-commands]

Verify all these scripts exist in `package.json`. Add any that are missing:

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx src/scripts/seed.ts"
  }
}
```

> Note: `db:*`, `test`, and `test:e2e` scripts will fail until their required packages are added in Stories 1.2+. Define them now so the architecture contract is complete.

---

### `tsconfig.json` — Path Alias Verification

[Source: architecture.md#coding-standards]

All imports in the codebase use `@/` resolving to `src/`. Verify `tsconfig.json` contains:

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

This is standard Next.js 14 setup — it is likely already configured, but must be verified.

---

### `.gitignore` Requirements

Verify `.gitignore` includes (add if missing):
```
.env*
!.env.example
node_modules/
.next/
out/
.DS_Store
```

---

### `.env.example` — Full Template

[Source: architecture.md#required-environment-variables]

Create `.env.example` with all required keys (no values):

```bash
# ─── Auth ─────────────────────────────────────────────────────────────────────
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=                        # openssl rand -base64 32
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ─── Database ─────────────────────────────────────────────────────────────────
DATABASE_URL=postgresql://postgres:[password]@db.[project].supabase.co:5432/postgres
# Use the pooler URL for production (port 6543, pgBouncer):
# DATABASE_URL=postgresql://postgres.[project]:[password]@aws-0-ap-south-1.pooler.supabase.com:6543/postgres

# ─── Cloudflare R2 ────────────────────────────────────────────────────────────
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=motoyaar-documents
R2_PUBLIC_URL=                          # e.g. https://documents.motoyaar.app

# ─── Email ────────────────────────────────────────────────────────────────────
RESEND_API_KEY=

# ─── AI ───────────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=

# ─── Push Notifications (VAPID) ───────────────────────────────────────────────
# Generate: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=mailto:support@motoyaar.app
NEXT_PUBLIC_VAPID_PUBLIC_KEY=           # Same as VAPID_PUBLIC_KEY (exposed to client)

# ─── Cron Security ────────────────────────────────────────────────────────────
CRON_SECRET=                            # openssl rand -base64 32

# ─── Admin Seed ───────────────────────────────────────────────────────────────
ADMIN_EMAIL=admin@motoyaar.app
ADMIN_PASSWORD=                         # Set securely; hashed on seed
```

---

### `vercel.json` — Cron Configuration

[Source: architecture.md#vercel-json-cron-configuration]

Create `vercel.json` in the project root:

```json
{
  "crons": [
    {
      "path": "/api/cron/document-expiry",
      "schedule": "30 0 * * *"
    }
  ]
}
```

> `30 0 * * *` = 00:30 UTC ≈ 06:00 IST.

---

### GitHub Actions CI Pipeline

[Source: architecture.md#cicd-pipeline]

Create `.github/workflows/ci.yml`:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Type check
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Unit tests
        run: pnpm test
```

---

### `src/lib/errors.ts` — Implement Now (Fully Functional)

[Source: architecture.md#18-error-handling-strategy]

Create `src/lib/errors.ts` exactly as defined in the architecture. This file is used by **every API route** across all epics.

```typescript
import { NextResponse } from "next/server";
import { ZodError } from "zod";

// ─── Error Response Shape ──────────────────────────────────────────────────
interface ApiErrorResponse {
  error: {
    code: string;
    message: string;
    details?: Record<string, unknown>;
    timestamp: string;
    requestId: string;
  };
}

// ─── Custom Error Classes ──────────────────────────────────────────────────
export class NotFoundError extends Error {
  constructor(message: string) {
    super(message);
    this.name = "NotFoundError";
  }
}
export class ForbiddenError extends Error {
  constructor(message: string) {
    super(message);
    this.name = "ForbiddenError";
  }
}
export class ConflictError extends Error {
  constructor(message: string) {
    super(message);
    this.name = "ConflictError";
  }
}
export class QuotaExceededError extends Error {
  constructor(message: string) {
    super(message);
    this.name = "QuotaExceededError";
  }
}

// ─── Central API Error Handler ─────────────────────────────────────────────
export function handleApiError(error: unknown): NextResponse {
  const requestId = crypto.randomUUID();
  const timestamp = new Date().toISOString();
  const base = { timestamp, requestId };

  if (error instanceof ZodError) {
    return NextResponse.json(
      { error: { code: "VALIDATION_ERROR", message: "Invalid input", details: error.flatten(), ...base } },
      { status: 422 }
    );
  }
  if (error instanceof NotFoundError) {
    return NextResponse.json(
      { error: { code: "NOT_FOUND", message: error.message, ...base } },
      { status: 404 }
    );
  }
  if (error instanceof ForbiddenError) {
    return NextResponse.json(
      { error: { code: "FORBIDDEN", message: error.message, ...base } },
      { status: 403 }
    );
  }
  if (error instanceof ConflictError) {
    return NextResponse.json(
      { error: { code: "CONFLICT", message: error.message, ...base } },
      { status: 409 }
    );
  }
  if (error instanceof QuotaExceededError) {
    return NextResponse.json(
      { error: { code: "QUOTA_EXCEEDED", message: error.message, ...base } },
      { status: 403 }
    );
  }

  console.error("[API Error]", error);
  return NextResponse.json(
    { error: { code: "INTERNAL_ERROR", message: "An unexpected error occurred", ...base } },
    { status: 500 }
  );
}
```

---

### `src/lib/api-client.ts` — Implement Now (Fully Functional)

[Source: architecture.md#frontend-api-client]

Create `src/lib/api-client.ts`. Used by all frontend `*Api.ts` wrappers across all epics.

```typescript
export class ApiError extends Error {
  constructor(
    public code: string,
    message: string,
    public details?: Record<string, unknown>,
    public status?: number
  ) {
    super(message);
    this.name = "ApiError";
  }
}

export async function apiRequest<T>(
  path: string,
  options?: RequestInit
): Promise<T> {
  const res = await fetch(`/api${path}`, {
    headers: { "Content-Type": "application/json" },
    ...options,
  });
  if (!res.ok) {
    const body = await res.json();
    throw new ApiError(
      body.error.code,
      body.error.message,
      body.error.details,
      res.status
    );
  }
  if (res.status === 204) return undefined as T;
  return res.json();
}
```

---

### `src/lib/logger.ts` — Implement Now (Fully Functional)

[Source: architecture.md#logging-pattern]

Install `pino` and `pino-pretty`, then create `src/lib/logger.ts`:

```bash
pnpm add pino
pnpm add -D pino-pretty
```

```typescript
import pino from "pino";

export const logger = pino({
  level: process.env.NODE_ENV === "production" ? "info" : "debug",
  ...(process.env.NODE_ENV !== "production" && {
    transport: { target: "pino-pretty" },
  }),
});

// Usage in API routes:
// logger.info({ userId, vehicleId }, "Vehicle created");
// logger.error({ error, requestId }, "AI report generation failed");
```

---

### `app/api/auth/[...nextauth]/route.ts` — Stub

[Source: architecture.md#nextauth-configuration]

Create a minimal stub for NextAuth route (required for the NextAuth package to resolve):

```typescript
// TODO: Implement in Story 2.1 — Authentication & Onboarding
// Full implementation: src/lib/auth.ts authOptions + this route handler
import NextAuth from "next-auth";

const handler = NextAuth({ providers: [] });
export { handler as GET, handler as POST };
```

---

### Stub File Pattern

[Source: architecture.md#12-unified-project-structure]

All stub files (service files, stores, hooks, api wrappers) follow this pattern:

```typescript
// TODO: Implement in Story X.X — [Story Title]
export {};
```

For stub lib files (auth.ts, db/client.ts, etc.):
```typescript
// TODO: Implement in Story X.X — [Story Title]
```

---

### Testing Requirements

[Source: architecture.md#16-testing-strategy]

This is a scaffold/infrastructure story. **No unit tests or E2E tests** are required. Testing infrastructure (Vitest, Playwright) is added in Story 1.2.

Verification is done by:
- `pnpm dev` — must start without runtime errors
- `pnpm typecheck` — must pass (stub files with `export {}` are valid TypeScript)
- `pnpm lint` — must pass

---

### Technical Constraints

[Source: architecture.md#3-tech-stack]

| Technology | Version | Notes |
|------------|---------|-------|
| Next.js | 14.2 | App Router — already installed |
| TypeScript | 5.4 | Already installed |
| Tailwind CSS | v3.4 | Already installed |
| pnpm | 9 | Package manager |
| Node.js | 20 | Runtime |
| pino | 9.x | New install this story |

---

### Coding Standards

[Source: architecture.md#17-coding-standards]

- `src/types/index.ts` is the **single source of truth for all types** — never import types from elsewhere or duplicate them
- All imports use `@/` alias (e.g. `@/lib/errors`, `@/types`)
- No `"use client"` directive unless the component uses hooks or browser APIs
- Environment variables accessed via `process.env.VARIABLE_NAME` directly — no wrapper needed

---

## Tasks / Subtasks

### Task 1 — Verify Base Scaffold (AC: 1, 4)
- [x] 1.1 Run `pnpm install` to ensure all existing dependencies are installed
- [x] 1.2 Run `pnpm dev` and confirm it starts without errors
- [x] 1.3 Verify `tsconfig.json` has `@/` path alias (`"paths": { "@/*": ["./src/*"] }`)
- [x] 1.4 Verify `tailwind.config.ts` and `components.json` exist
- [x] 1.5 Verify `public/manifest.json` exists

### Task 2 — Set Up `.gitignore` and `.env.example` (AC: 2)
- [x] 2.1 Verify/update `.gitignore` to exclude `.env*` (except `.env.example`), `node_modules/`, `.next/`
- [x] 2.2 Create `.env.example` using the full template from Dev Notes

### Task 3 — Add `package.json` Scripts (AC: 4, 5, 6)
- [x] 3.1 Add missing scripts to `package.json`: `typecheck`, `test`, `test:watch`, `test:e2e`, `db:generate`, `db:migrate`, `db:studio`, `db:seed`
- [x] 3.2 Verify `dev`, `build`, `start`, `lint` scripts exist

### Task 4 — Create Core Infrastructure Files (AC: 3, 5)
- [x] 4.1 Install pino: `pnpm add pino && pnpm add -D pino-pretty`
- [x] 4.2 Create `src/lib/errors.ts` — full implementation from Dev Notes (error classes + `handleApiError`)
- [x] 4.3 Create `src/lib/api-client.ts` — full implementation from Dev Notes (`ApiError` + `apiRequest`)
- [x] 4.4 Create `src/lib/logger.ts` — full implementation from Dev Notes

### Task 5 — Create Missing Pages and Route Stubs (AC: 3)
- [x] 5.1 Create stub pages: `src/app/(auth)/onboarding/page.tsx`
- [x] 5.2 Create stub pages: `src/app/(app)/garage/new/page.tsx`, `src/app/(app)/garage/[id]/page.tsx`, `src/app/(app)/garage/[id]/edit/page.tsx`
- [x] 5.3 Create stub pages: `src/app/(app)/community/[id]/page.tsx`
- [x] 5.4 Create stub pages: `src/app/(app)/profile/settings/page.tsx`
- [x] 5.5 Create stub admin pages: `src/app/admin/layout.tsx`, `src/app/admin/page.tsx`, `src/app/admin/reported/page.tsx`, `src/app/admin/users/page.tsx`, `src/app/admin/settings/page.tsx`
- [x] 5.6 Create NextAuth route stub: `src/app/api/auth/[...nextauth]/route.ts`

### Task 6 — Create Directory Structure and Service Stubs (AC: 3)
- [x] 6.1 Create component subdirectories (with `.gitkeep`): `src/components/vehicles/`, `documents/`, `expenses/`, `trips/`, `community/`, `reports/`, `notifications/`, `onboarding/`
- [x] 6.2 Create lib stubs: `src/lib/auth.ts`, `src/lib/db/client.ts`, `src/lib/db/schema.ts`, `src/lib/r2.ts`, `src/lib/resend.ts`, `src/lib/anthropic.ts`, `src/lib/push.ts`
- [x] 6.3 Create service stubs (all files in `src/services/` listed in architecture)
- [x] 6.4 Create frontend API wrapper stubs (`src/services/api/*.ts`)
- [x] 6.5 Create store stubs: `src/stores/notificationStore.ts`, `src/stores/uiStore.ts`
- [x] 6.6 Create hook stubs: `src/hooks/useVehicles.ts`, `src/hooks/usePushSubscription.ts`, `src/hooks/useNotifications.ts`
- [x] 6.7 Create util stubs: `src/utils/currency.ts`, `src/utils/date.ts`, `src/utils/hotScore.ts`
- [x] 6.8 Create `src/lib/validations/` directory
- [x] 6.9 Create empty dirs: `drizzle/migrations/`, `e2e/`

### Task 7 — CI/CD and Deployment Config (AC: 3)
- [x] 7.1 Create `vercel.json` with cron configuration from Dev Notes
- [x] 7.2 Create `.github/workflows/ci.yml` from the exact template in Dev Notes

### Task 8 — Final Verification (AC: 1, 4, 5, 6)
- [x] 8.1 Run `pnpm dev` — must start without runtime errors
- [x] 8.2 Run `pnpm typecheck` — must pass (stub `export {}` files are valid)
- [x] 8.3 Run `pnpm lint` — must pass; fix any lint errors

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- `next.config.ts` renamed to `next.config.mjs` — Next.js 14.2 does not support `.ts` config files
- ESLint not pre-installed; added `eslint@8` + `eslint-config-next@14.2.0` + `.eslintrc.json` (newer eslint-config-next@16 is incompatible with Next.js 14.2)
- pnpm not on PATH in bash shell; installed via `npm install -g pnpm`
- All stub pages, services, hooks, stores, and utils created per architecture spec
- All three core lib files (errors.ts, api-client.ts, logger.ts) fully implemented

### Debug Log References
- ESLint v10 installed initially → circular reference error → downgraded to eslint@8 + eslint-config-next@14.2.0
- next.config.ts unsupported in Next.js 14.2 → converted to next.config.mjs

### File List
**Created:**
- `next.config.mjs` (replaced next.config.ts)
- `.gitignore`
- `.env.example`
- `.eslintrc.json`
- `vercel.json`
- `.github/workflows/ci.yml`
- `src/lib/errors.ts`
- `src/lib/api-client.ts`
- `src/lib/logger.ts`
- `src/lib/auth.ts`
- `src/lib/db/client.ts`
- `src/lib/db/schema.ts`
- `src/lib/r2.ts`
- `src/lib/resend.ts`
- `src/lib/anthropic.ts`
- `src/lib/push.ts`
- `src/app/(auth)/onboarding/page.tsx`
- `src/app/(app)/garage/new/page.tsx`
- `src/app/(app)/garage/[id]/page.tsx`
- `src/app/(app)/garage/[id]/edit/page.tsx`
- `src/app/(app)/community/[id]/page.tsx`
- `src/app/(app)/profile/settings/page.tsx`
- `src/app/admin/layout.tsx`
- `src/app/admin/page.tsx`
- `src/app/admin/reported/page.tsx`
- `src/app/admin/users/page.tsx`
- `src/app/admin/settings/page.tsx`
- `src/app/api/auth/[...nextauth]/route.ts`
- `src/services/userService.ts`, `vehicleService.ts`, `documentService.ts`, `expenseService.ts`, `tripService.ts`, `reportService.ts`, `aiReportService.ts`, `communityService.ts`, `notificationService.ts`, `storageService.ts`, `emailService.ts`, `pushService.ts`, `adminService.ts`, `cronService.ts`
- `src/services/api/vehicleApi.ts`, `documentApi.ts`, `expenseApi.ts`, `tripApi.ts`, `reportApi.ts`, `communityApi.ts`, `notificationApi.ts`
- `src/stores/notificationStore.ts`, `uiStore.ts`
- `src/hooks/useVehicles.ts`, `usePushSubscription.ts`, `useNotifications.ts`
- `src/utils/currency.ts`, `date.ts`, `hotScore.ts`
- Component dirs with `.gitkeep`: `src/components/vehicles/`, `documents/`, `expenses/`, `trips/`, `community/`, `reports/`, `notifications/`, `onboarding/`
- Empty dirs: `src/lib/validations/`, `drizzle/migrations/`, `e2e/`

**Modified:**
- `package.json` — added scripts + pino dependency
- `docs/stories/1.1.story.md` — status + task checkboxes

**Deleted:**
- `next.config.ts`

### Change Log

| Date | Change | Reason |
|------|--------|--------|
| 2026-03-15 | Story created | Initial draft by SM (Bob) |
| 2026-03-15 | Story implemented | Dev agent (James Bond) — all tasks complete |

---

## QA Results

**Reviewer:** Quinn (QA Agent)
**Date:** 2026-03-15
**Gate Decision:** ✅ PASS
**Gate File:** `qa/gates/1.1-initialize-nextjs-project.yml`

| AC | Result | Evidence |
|----|--------|----------|
| AC1 — Next.js + TS + ESLint + Tailwind | ✅ PASS | Dev server starts; all tooling verified |
| AC2 — .gitignore | ✅ PASS | `.env*`, `node_modules/`, `.next/` all present |
| AC3 — Project structure complete | ✅ PASS | 27 spot-checked files confirmed; all stubs created |
| AC4 — `pnpm dev` starts without errors | ✅ PASS | Next.js 14.2.35 — Ready in 2.9s |
| AC5 — `pnpm typecheck` passes | ✅ PASS | `tsc --noEmit` exits 0 |
| AC6 — `pnpm lint` passes | ✅ PASS | No ESLint warnings or errors |

**Notes:** No defects found. Dev agent correctly resolved two infrastructure issues (next.config.ts → .mjs; ESLint not pre-installed). All fixes are sound and aligned with Next.js 14.2 constraints.